# M2 回放与收拾 · SPEC（开发规格 v1）

> 对应《项目需求文档.md》里程碑 M2。实现与测试以本文档为锚。
> 范围：轨迹平滑/抽稀/分段（算法层，本轮）、轨迹回放动画与回顾页收拾 UI（下一步）。
> **范围修正**：漏访检查（F-9）推迟到 M4——它依赖"今年拜年清单"（M4 的 F-1），M2 阶段没有清单可比对。

## 1. 算法层（本轮实现，全部纯函数 + TDD）

### 1.1 跳变切段 `jumpSplit`
- 输入原始轨迹点（含 `jump` 标记，M1 已标：<2s 内位移 >100m）；
- 在 jump 点处切段，jump 点归入**后段**开头；无 jump 则整条为一段。

### 1.2 段内滑动平均 `movingAverage(w=5)`
- 每点取 ±2 窗口内坐标均值，**窗口不跨段**（先切段再平滑），**窗口内跳过 jump 点，jump 点与段端点保持原坐标**（防轨迹两端被均值拉短——拉短后实测距离可能小于直线距离，污染边权）；
- 输出长度与输入一致，保留原 `t`/`acc`/`jump`。

### 1.3 平滑管线 `smoothTrack`
- = `jumpSplit` 后逐段 `movingAverage`，再拍平。长度不变。

### 1.4 抽稀 `douglasPeucker(epsM=2)`
- 经典递归实现；距离用**等距圆柱平面近似**（村庄尺度几公里内误差可忽略）；
- 保证首尾点保留；`eps=2m` 保留形状、滤 GPS 噪声。

### 1.5 轨迹分段 `buildEdges(session)`
- 停靠序列 `stops` = `home@首点时间` + `visits(按 arriveT 排序)` + `home@末点时间(仅已结束)`；
- 边 i：from/to 节点 id、出行方式（**进入**该停靠点那次访问的 mode）、时间窗 `(departT, arriveT]` 内的原始点、平滑点链、距离（平滑链 haversine 逐段求和）；
- 中途回 Home 的多段循环天然成立（visits 里含 home）。

### 1.6 SVG 投影 `boundsOf / projectToView / toSvgPath`
- 球面坐标 → 平面视口：先求边界框，等比缩放居中（北在上、留白 10%），输出 SVG path 字符串。

## 2. UI 层（下一步实现，不走单测，走真机/浏览器验收）

- **回放页**：平滑抽稀后的路径描边动画 + 行进光点，速度 1x/2x/4x；
- **回顾页·收拾**：节点列表改名（自由文本）、选中两节点合并、从节点间拆分、异常样本（jump/长耗时）列表可剔除；
- 入口：结束拜年后自动进入回放；主页"历史记录"进入回顾页。

## 3. 测试清单（vitest，本轮全做）

1. `jumpSplit`：无 jump 单段 / 中间 jump 切两段 / 开头 jump 不产生空段；
2. `movingAverage`：三点共线中间点不变 / 长度不变 / 端点窗口收窄；
3. `smoothTrack`：长度不变 / 窗口不跨 jump 段；
4. `douglasPeucker`：直线仅两端点 / eps=0 全保留 / 直角拐点保留 / 空与两点直通；
5. `buildEdges`：三段场景（home→A→B→home）边数/时间窗内点数/距离为正/出行方式归属；中途回 Home 多段循环；
6. `projectToView`：所有点落在视口内 / 等比不变形 / 北在上；
7. `toSvgPath`：起笔 M、点数一致。

## 4. 验收

- 电脑浏览器：导入构造的示例 JSON 回放动画正常、收拾操作（改名/合并/拆分）生效；
- 真机：走一段真实轨迹后回放与实际路径一致。
