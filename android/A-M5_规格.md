# A-M5 打磨互通 · 规格（Android 版）

> 对应 `android/SPEC.md` §7。最后一个里程碑：把"可用的 App"变成"可交付、可互通、可验收"的 App。

## 1. 范围

- **应用图标**：灯笼 PNG 生成五档 mipmap（48/72/96/144/192），manifest 接入；
- **互通闭环**：历史页「导出 JSON」（FileProvider 分享）与「导入 JSON」（系统文件选择器）——与网页版同一格式（`android/数据格式.md` 契约）；
- `JsonStore.importAllJson`（契约测试：导出→导入往返逐字段一致）；
- **三份交付文档**：本规格、真机验收清单、高德 Key 接入指引；
- 视觉微调：保持 A-M1~A-M4 的暖色基调，不再大改。

## 2. 测试

- core 新增 1 项：导入导出往返（互通契约）；
- 累计：**core 107 + app 8 = 115 项**。

## 3. 验收

- 云端：115 项全绿 + APK 含图标构建成功；
- 真机：按《真机验收清单》执行——**这是整个 Android 迁移的最终门禁**。
