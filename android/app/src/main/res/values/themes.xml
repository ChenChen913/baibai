<resources>
    <!-- 暖色年味主题（无 Material XML 依赖，Compose 侧另有配色） -->
    <style name="Theme.Baibai" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">#FFF8EE</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
