# M4 跨年便利 · SPEC（开发规格 v1）

> 对应《项目需求文档.md》里程碑 M4：今年清单（出门前，F-1）、漏访检查（回顾页，F-9，自 M2 推迟至此）、套名继承（回顾页，D17）、历年成绩单列表（历史页，F-15）。

## 1. 数据模型

- IndexedDB 新增 store **`plans`**（keyPath=`year`，一年一份）：`{ year, items: [{ name, pos }], createdAt, updatedAt }`；
- `DB_VERSION` 1 → **2**（onupgradeneeded 补建新 store，旧数据不受影响）。

## 2. 纯函数层（plan.ts，全部 TDD）

| 函数 | 语义 |
|---|---|
| `planFromSession(prev, year)` | 从去年会话生成今年清单：items = 去年 nodes 的 `{name, pos}`（name 可能为空，UI 显示"户N"兜底） |
| `matchPlan(session, plan, threshold=10m)` | **贪心一对一匹配**：item×node 距离升序，≤10m 配对（每方最多配一次）→ `{visited:[{item,nodeId}], missing:item[]}`；missing 即"疑似漏访" |
| `nameCandidates(nodePos, prevNodes, top=3)` | 套名候选：去年有名字的户按距离升序取前 3（空名不参与） |

## 3. UI（下一步实现，浏览器/真机验收）

- **记录页（IDLE）**：清单入口 → 今年清单管理页：显示今年 items、增删改名、按钮"从去年导入"（取最近一个往年会话生成）；
- **回顾页**：① 漏访检查区（今年有清单时）：missing 标红"XX 没去！"；② 收拾区"套用去年的户名"：对每个今年节点弹候选（前 3，按距离），逐个确认或跳过（D17）；
- **历史页**：历年成绩单——每场会话一行：日期 · 户数 · 距离 · 绕路率（savingsTimePct）。

## 4. 测试清单（本轮全做）

1. `planFromSession`：节点数一致、名字原样保留；
2. `matchPlan`：一对一贪心（两 item 争同一 node 只配最近者）、漏访、10m 边界（9.999 配 / 10.001 缺）、空清单/空会话；
3. `nameCandidates`：按距离升序、空名过滤、top 截断；
4. db plans：保存/读取/覆盖/清除/列表；版本升级后旧 store 数据仍在。
