# baibai项目 · 拷问决策记录（grill log）

> 本文件是项目从 0 到 1 的决策树档案。每轮拷问更新一次：锁定一项决策，或砍掉一个分支。
> 规则：一次只解决一个决策；每个决策都记录「结论 + 理由 + 代价」。

## 项目一句话

乡村拜年轨迹复盘玩具：大年初一走村串户时用手机记录真实行走轨迹，用「暂停/继续」标记逗留的每一户；拜完年后回放当日路线，用路径规划算法算出**当日最优走法**与**飞行直线最优**，三线动画对比——看看自己今年绕了多少路。年度独立、每年一局、纯本地、自己玩。

## 已锁定决策

| # | 决策 | 结论 | 理由 | 接受的代价 |
|---|---|---|---|---|
| D0 | 项目动机 | 个人兴趣，自己做着玩 | 用户自述"实际用处可能没有那么大" | 不做商业化、不做多用户、不追求真实用户数 |
| D1 | 产品形态 | **网页版 PWA**（纯前端、零后端、GitHub Pages 部署） | 唯一用户 + 零分发成本 + 设计自由度最高 + PWA 可离线（村里信号差） | 记录轨迹时手机需保持亮屏（锁屏冻结 GPS） |
| D2 | 数据存储 | 手机本地 IndexedDB + 导出 GPX/JSON 备份 | 零服务器、换手机不丢数据、隐私全在自己手里 | 无云同步、无多设备 |
| D3 | 离线能力 | Service Worker 全量缓存，断网可跑 | 拜年当天信号差是常态 | 无 |
| D4 | 定位方案 | 手机自带 GPS（含北斗），无需 RTK/外接设备 | 户间距 200~500m，GPS 精度 3~10m，冗余 20~50 倍；农村天空开阔是最优环境 | 进屋丢星（与"暂停"逻辑天然兼容）；出门后 3~10s 重新锁星 |
| D5 | 展示风格 | 运动 APP 式：拜完年后**实际路径动态回放**；**优化路径动态推演**（动画） | 用户明确要求："页面不美观，做它也没意思" | 动画是核心工作量之一 |
| D6 | 绘制方案 | 实际路径=真实地图轨迹线（Leaflet+OSM 在线时）；优化路径=自绘 SVG 节点连线示意图（离线兜底） | 示意图最炫技、完全可控、不依赖地图覆盖 | 村庄 OSM 瓦片覆盖可能稀薄（不致命） |
| D7 | 路径视图（边来源） | **三层视图全要**，不是三选一：① 真实轨迹视图（平滑后的实走路线）；② 地图路网视图（OSM 吸附，在线增强）；③ 几何飞行视图（点+直线，"坐飞机拜年"压轴效果） | 用户要求"好玩"：最后路径变成点和线、标准几何图形 | 视图逻辑更复杂（同一数据三种渲染） |
| D8 | 优化模式 | **算两次，两个模式**：① 实际路线（走路距离加权）；② 飞行路线（直线距离加权），各算各的最优顺序，附对比卡（"如果能飞，少走 X km"） | 用户定调"主要就是玩儿"；同一求解器换权重矩阵，成本近零 | 两条最优顺序可能不同，需在 UI 上明确区分两种模式 |
| D9 | 起终点 | 起点=终点=**Home**（默认预设节点），闭环回路：Home → 全部农户 → Home | 用户确认；固定 depot 还简化了求解（Held-Karp 固定起点） | 无 |
| D10 | 节点合并阈值 | **≤10m 合并为同一节点，>10m 为新节点**（含 Home 起止偏差）；回顾页手动合并/拆分/纠错为必备功能 | 用户拍板 10m：误拆分可修复（重复节点良性），误合并丢数据（拜年对象消失）；GPS 晴空噪声 3~10m，10m 贴着噪声边界 | 每年可能产生少量重复节点，需手动收拾 |
| D11 | 节点命名 | 到达按暂停时**自动编号**（按拜访顺序 1、2、3…）；可当场改名，也可事后回顾页自由改；名字不限数字（文字/字母/数字组合） | 现场交互压到最低，命名的乐趣留到回顾页 | 无 |
| D12 | 数据存储模型 | IndexedDB 三张表：**nodes**（户：稳定 id + 名字 + positions[] 逐年坐标样本 + 中心点）、**tracks**（路：年份 + 原始/平滑轨迹 + 分段边）、**sessions**（场次：年份 + 拜访顺序 + 到达/离开时刻）；换手机导出单个 JSON 全量备份 | 跨年对应问题需要"样本簇"而非单点坐标；本地零服务器 | 无云同步 |
| D13 | 跨年关系（B2 修订） | **年度独立**：往年数据不参与当年优化，跨年不自动匹配/合并；当年节点一律重新自动编号；户名继承方式待定（拷问中） | 用户拍板"每一年有每一年的路径，不要管上一年"；彻底消除跨年匹配失败的担忧 | 每年需重新命名（或等继承方案） |
| D14 | 优化路线集 | **三条最优线**：① 飞行模式=直线距离最优（精确）；② 普通模式=时间最优（当日复盘主展示线，未走过边用速度折算）；③ 普通模式=距离最优（未走过边用直线×绕行系数估算）。三线并呈对比卡 | 用户拍板"距离和时间都要有"；同一求解器三次换权重，成本近零 | 时间线/距离线的未走过边为估算值，需在 UI 标注"估算" |
| D15 | 边权聚合（年度内） | 当年数据独立；同一年内同一条边若走过多次，**取最短耗时那次**（A 方案）；明显异常样本（如中途聊天 15 分钟）可人工剔除 | 用户选 A；年度独立使跨年聚合问题消失 | 当年没走过的边只能估算：飞行线精确（直线可算），走路距离线部分估算，走路时间线估算最多（无法几何折算） |
| D16 | 优化结果定位 | **年度复盘（what-if）**：拜完年后回答"如果我今天按最优顺序走，能省多少？"——当日实走 vs 当日最优 vs 飞行最优；年度成绩单，明年重新录、重新比 | 用户拍板 a；与年度独立 + 好玩优先完全一致；"明年路线"需要历史累积，逻辑上与年度独立矛盾 | 无"明年路线规划"功能 |
| D17 | 户名继承 | **b 可选一键继承**：回顾页按钮"套用去年的户名"，按地理位置把候选户推给今年节点（最近 3 户排序），**逐个人工确认**，跳过即保持编号；不点按钮则与去年完全无关 | 唯一同时满足：年度独立默认保留 + 省 80% 重复打字 + 错配风险被逐个确认压到零 | 每年仍需几十秒点确认 |
| D18 | 拜年清单 | **a 全自动防漏**：出门前"生成今年清单"（导入去年名单，可增删）；拜年当天只管按暂停；回顾页自动对比"计划 vs 实际"，漏访标红"XX 没去！"并自动从套名候选剔除 | 漏拜=得罪人；现场零负担；复用 D17 匹配机制 | 无 |
| D19 | 记录体验（R1+R2+R3） | **R1=b 混合出行**：每次离开一户按"继续"时，按钮旁小车图标；默认走路，骑车段出门前点一下切骑车，到下一家自动回走路；**R2 撤销**：录音页撤销上一暂停点 + 回顾页合并/拆分兜底；**R3 反馈**：开始/结束/到达暂停时震动+轻音效（可关） | 还原"大部分走、偶尔回家取车骑"的真实场景；成绩单增加步行/骑行里程分解 | 骑车日需点 2~4 次小车图标 |
| D20 | 中途回家 | 允许当天多段循环（Home→A→B→Home→骑车→C→Home…）；最优对比永远按**单环**（Home→全部农户各一次→Home）计算，中间回程计入"多绕的路" | 用户真实行为；单环对比才有意义 | 无 |
| D21 | 视觉主题（U） | **大白天的暖色年味**：浅暖色底（米白/淡金）+ 红色系柔和渐变；边缘点缀虚化低透明的**礼花+炮仗**（只用这两样，不进主体）；整体明亮优雅，不做暗色；步行场景=暖底+朱红轨迹线，飞行压轴=晴空+鎏金节点+金红航线；成绩单=浅色玻璃拟态卡片 | 用户拍板：拜年是大白天，不要黑夜模式；元素 1~2 样即可，多则乱 | 无 |
| D22 | 长时间运行可靠性 | 目标：2 小时+ 不卡死，卡死也不丢数据。① 重计算（平滑/抽稀/Held-Karp/morph 预处理）全部放 Web Worker，主线程只做 UI；② GPS 点内存缓冲、每 5~10s 或按键时批量落盘，不逐点写库；③ **会话检查点**：每 10s 或每次按键持久化完整状态，异常退出后重开可续录；④ Wake Lock 保持亮屏（iPhone 手动设自动锁定=永不）；⑤ 全局错误边界：未捕获异常记日志+提示，绝不静默冻结；⑥ 不录音频，2 小时 GPS 数据仅几十 KB，无内存压力 | 用户担心 2 小时运行卡死（如某户停留 20 分钟后再操作）；暂停期=停 GPS+零计算，是最安全状态 | 浏览器/系统级故障仍可能，靠检查点兜底"不丢数据" |

## 技术栈（依据 D1~D6）

| 层 | 选型 |
|---|---|
| 形态 | 网页 PWA（GitHub Pages 托管，自动 HTTPS） |
| 地图 | Leaflet + OSM 瓦片（在线增强） |
| 核心图 | 自绘 SVG/Canvas 节点-连线图（主展示，离线可用） |
| 定位 | 浏览器 Geolocation API（精度由手机硬件决定） |
| 存储 | IndexedDB（本地）+ GPX/JSON 导入导出 |
| 离线 | Service Worker |
| 算法 | 纯 JS：Douglas-Peucker 简化 + 平滑滤波 + Dijkstra/Floyd + Held-Karp（n≤20 精确解 TSP） |
| 视觉 | 现代 CSS（深色玻璃拟态）+ SVG/CSS 动画 |

## 已锁定的视觉要求（D5 补充）

- **U1 压轴动画**：拜完年后，真实轨迹**变形（morph）为点线几何图形**——轨迹线收缩成直线，节点浮现，如同"切换到飞行视角"。这是全页的高潮镜头。
- **U2 实际路径动态回放**：运动 APP 式，沿轨迹线行进动画。
- **U3 优化路径动态推演**：按拜访顺序逐段点亮 + 序号标注。

## 待拷问分支（决策树未定节点）

- **无。** 全部 22 项决策（D0~D22）已锁定并经用户"全部确认"，拷问结束（2026-08-04）。

## 拷问轮次记录

- 第 1~3 轮：确认项目对象（空文件夹=全新项目，此前误判作废）。
- 第 4 轮：锁定 D0 动机（个人兴趣）→ 问题 2 抛出形态分支。
- 第 5 轮：锁定 D1 形态（网页版 PWA，用户确认接受亮屏代价）、D4 定位方案、D6 绘制方案。
- 第 6 轮：锁定 D5 展示风格（运动 APP 动画）、D2/D3 存储与离线；抛出边来源问题（H）。
- 第 7 轮：锁定 D7 三层路径视图（真实轨迹/OSM 路网/几何飞行）+ U1~U3 动画要求；抛出 H2（优化算几次）。
- 第 8 轮：锁定 D8 双模式优化（实际/飞行）、D9 起终点=Home 闭环、D10 起止偏差 30m 内合并；抛出 B（节点识别）。
- 第 9 轮：D10 修正为 10m 合并阈值（误拆分优于误合并）；锁定 D11 自动编号+自由改名；抛出 B2（跨年同户识别）。
- 第 10 轮：锁定 B2=C 混合方案（≤10m 自动匹配 + 回顾页认领弹窗 + 坐标追加不覆盖，防漂移三层防御）→ 记入 D12/D13；抛出 D 边权（时间/距离）。
- 第 11 轮：锁定 D14 三条最优线（飞行距离 / 走路时间 / 走路距离）；抛出边权聚合（多年同边取哪次）。
- 第 12 轮：用户拍板**年度独立**（不要管上一年）→ D13 修订、锁定 D15（年度内取最短耗时 A 方案 + 异常可剔除）；抛出"优化结果定位"（复盘今年 vs 明年规划）。
- 第 13 轮：锁定 D16 年度复盘定位（当日实走/当日最优/飞行最优三线对比）；重写项目一句话；抛出户名继承。
- 第 14 轮：锁定 D17 户名继承=b（一键继承+逐个确认）；抛出拜年清单。
- 第 15 轮：锁定 D18 拜年清单=a（全自动防漏）；抛出记录体验（R）。
- 第 16 轮：锁定 D19 记录体验（R1=b 混合出行小车图标 / R2 撤销 / R3 震动音效）+ D20 中途回家多段循环与单环对比；抛出视觉细节（U）。
- 第 17 轮：锁定 D21 视觉主题（白天暖色年味：米白金底+红渐变+边缘虚化礼花炮仗）；全部 21 项决策锁定，进入收束确认。
- 第 18 轮：用户"全部确认"；追加 D22 长时间运行可靠性（Web Worker/批量落盘/检查点续录/Wake Lock/错误边界）；**拷问完成，共识达成**。
