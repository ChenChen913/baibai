# M1 记录闭环 · SPEC（开发规格 v1）

> 对应《项目需求文档.md》里程碑 M1。本文档是写代码前的"锚"：实现与测试都以它为准，冲突时改文档再改代码。
> 范围：只做"能完整记录一天拜年并安全保存"。平滑、回放、优化、清单、视觉精修全部属于 M2~M5。

## 1. 工程设置

| 项 | 决策 |
|---|---|
| 语言/构建 | TypeScript（strict）+ Vite + vitest；纯前端无框架 |
| 目录 | `src/`：`geo.ts`（球面距离）、`state.ts`（状态机）、`gps.ts`（采样/缓冲）、`db.ts`（IndexedDB+检查点）、`ui.ts`、`main.ts`；`tests/` 与 src 平行 |
| 仓库 | GitHub 仓库（公开），**从 M1 就启用 GitHub Pages**——真机 GPS 测试需要 HTTPS 安全上下文（手机局域网 IP 是"不安全上下文"，Geolocation 会被浏览器禁用），Pages 是最省事的正路；`git push` 即真机可测 |
| 版本 | 每完成一个可运行单元就提交；changelog 追加到 README |

## 2. 数据模型（TypeScript 接口）

```ts
interface LatLng { lat: number; lng: number; }
interface TrackPoint { t: number; pos: LatLng; acc: number; seg: string; }  // acc=精度(m)，seg=段id
interface Visit { nodeId: string; arriveT: number; leaveT: number; mode: 'walk'|'bike'; }
interface HouseNode { id: string; name: string; autoNo: number; pos: LatLng; }  // Home 是特殊节点 id="home"
interface Session {
  id: string; year: number; date: string;
  home: LatLng; nodes: HouseNode[]; visits: Visit[];
  points: TrackPoint[];           // 原始轨迹点（M2 才做平滑）
  state: SessionState; currentMode: 'walk'|'bike';
  createdAt: number; updatedAt: number; finished: boolean;
}
```

IndexedDB 两个 store：
- `active`：当前活跃会话快照（检查点，同键覆盖写）；
- `sessions`：已结束的历史会话（含 years 索引）。

## 3. 状态机（state.ts）

| 状态 | 含义 |
|---|---|
| `IDLE` | 未开始 |
| `WALKING` | 移动中（GPS 采样开启） |
| `PAUSED` | 在某户（GPS 采样关闭，最安全状态） |
| `FINISHED` | 已结束保存 |

转移表（按键驱动）：
- `IDLE --开始拜年--> WALKING`：创建 session，Home 坐标=前 3 个有效 fix 的中位数（若无 fix 用最后已知位置并标 acc）。
- `WALKING --暂停--> PAUSED`：创建节点（规则见 §4）+ Visit.arriveT；震动。
- `PAUSED --继续--> WALKING`：Visit.leaveT=now；段 id 自增；震动。
- `PAUSED --结束拜年--> FINISHED`：走 Home 校验（§5）。
- `WALKING --撤销--> （上一状态）`：删除最近一次 Visit；若该节点本次会话新建且已无任何 Visit，删除节点；编号留空号（M2 回顾页可重排）。撤销后再撤销=继续回溯。
- 任何状态 --意外中断--> 检查点恢复（§6）。

## 4. 节点创建与 10m 合并（D10）

- 暂停时节点坐标 = **最近 3 个有效 fix 的中位数**（抗单点跳变）；
- 与已有节点逐个算 haversine 距离：**≤10m → 不新建，Visit 挂到该节点**（同天重复到访，中途回家天然成立）；**>10m → 新建**，autoNo = 当前节点数（不含 Home）+ 1；
- 边界情况（必须测试）：① 与 Home ≤10m 的暂停算"回 Home"；② 两节点相距恰 10.0m（用 ≤ 判）；③ 精度差（acc>50m）的 fix 不参与中位数，全差则用原始点但节点标 `lowAcc`。

## 5. Home 起止（D9/D10）

- 结束时取最后 3 个 fix 中位数，与 Home 距离 ≤10m → 正常结束；>10m → 弹提示"当前位置距 Home X 米，仍要结束？"（允许强制结束并标记，M2 回顾页可纠错）。

## 6. 检查点与恢复（D22）

- 触发：每 10s + 每次按键（开始/暂停/继续/撤销/结束）→ 写 `active` 快照（整体写，数据量几十 KB 级）；
- 启动时若 `active` 存在且未 finished → 弹"检测到未完成的记录：继续 / 放弃"；
- 恢复语义：状态、节点、访问、轨迹点、当前模式全部还原；GPS 重新获取期间显示"正在定位…"，计时不回溯。

## 7. GPS 采样与缓冲（D22）

- `watchPosition(enableHighAccuracy: true)`；回调即记点（无频率控制，浏览器约 1Hz）；
- `WALKING` 状态记点，`PAUSED` 状态 `clearWatch`（省电 + 防屋内漂移污染轨迹）；
- 点先入内存数组，**每 5s 或按键时批量 flush** 到 IndexedDB，不逐点写库；
- 跳变防护（M1 最小版）：与上一点距离 >100m 且间隔 <2s 的点标 `jump`（不删除，M2 平滑时处理）。

## 8. 震动与亮屏

- `navigator.vibrate(50)`：暂停/继续/结束；
- `navigator.wakeLock.request('screen')`：开始拜年时申请，结束释放；iOS Safari 不支持 → 部署文档注明手动设"自动锁定=永不"。

## 9. JSON 导出

- 结束页 + 主页按钮"导出数据"：把 `sessions` 全部导出为一个 `.json` 下载；导入功能放 M2（回顾页做）。

## 10. UI（M1 基础版，视觉精修在 M5）

暖色底 + 大状态字 + 大按钮，一个页面：
- 状态区：`待机 / 🚶 记录中(已拜访 N 户) / 🏠 在某户 / ✅ 已保存`，运行时长计时；
- 按钮区（按状态显隐）：开始拜年 / 暂停 / 继续 / 撤销 / 结束拜年；
- 小工具条：走路/骑车切换（D19 小车图标，默认走路）、导出数据。

## 11. 测试清单（vitest，算法层 TDD）

1. `haversine` 距离（含 0 距离、跨经度、10.0m 边界）；
2. 状态机全部转移 + 非法转移拒绝（如 WALKING 直接 FINISHED）；
3. 10m 合并：新建/合并/恰 10m/低精度 fix/合并到 Home；
4. 撤销链：删 Visit、删孤儿节点、留空号、连续撤销；
5. 检查点：序列化往返一致、恢复语义正确；
6. Home 起止校验（≤10m 通过 / >10m 警告）；
7. 编号：按拜访顺序、不含 Home。

## 12. 真机验收清单

- 手机浏览器（Android Chrome + iPhone Safari）打开 Pages 地址；
- 模拟走 30 分钟：开始 → 走 → 暂停×3 户 → 撤销 1 次 → 继续 → 结束；
- 中途杀浏览器 → 重开 → "继续未完成记录" → 数据完整；
- 锁屏再亮屏（iOS 手动永不自动锁）→ 记录未断；
- 导出 JSON 成功下载。

## 13. M1 明确不做

- 不做平滑/抽稀（M2）、回放动画（M2）、优化算法（M3）、清单与套名（M4）、Service Worker 离线（M5）、视觉精修（M5）。
